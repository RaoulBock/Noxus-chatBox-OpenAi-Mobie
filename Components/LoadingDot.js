import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LoadingDot = () => {
  return (
    <View>
      <Text>LoadingDot</Text>
    </View>
  );
};

export default LoadingDot;

const styles = StyleSheet.create({});
